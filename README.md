# 🐾 Smart Pet Feeder & Enviroment (IoT ESP32)
เครื่องให้อาหารสัตว์เลี้ยงอัจฉริยะ ควบคุมผ่านอินเทอร์เน็ตและทำงานอัตโนมัติ (IoT Smart Pet Feeder) พัฒนาด้วยบอร์ดไมโครคอนโทรลเลอร์ ESP32

## ✨ ฟีเจอร์หลัก (Features)
* 🥣 **ควบคุมการให้อาหาร:** สั่งให้อาหารผ่านหน้าเว็บเพจได้ทันที (ทำงานด้วย Servo Motor)
* 📊 **ตรวจสอบปริมาณอาหาร:** วัดระดับอาหารในถังด้วยเซ็นเซอร์ Ultrasonic (HC-SR04) โชว์ผลเป็นเปอร์เซ็นต์ (0-100%)
* 🌡️ **ควบคุมอุณหภูมิอัตโนมัติ:** วัดอุณหภูมิและความชื้นด้วยเซ็นเซอร์ SHTC3 พร้อมสั่งเปิด/ปิดพัดลมระบายอากาศอัตโนมัติเมื่ออากาศร้อนเกินกำหนด
* 🔔 **แจ้งเตือนผ่าน Telegram:** ส่งข้อความแจ้งเตือนเข้ามือถือเมื่อมีการให้อาหาร หรือเปิด/ปิดพัดลม
* 🌐 **Firebase Integration:** เก็บข้อมูลประวัติย้อนหลังและทำงาน
* 📴 **Offline Mode:** หากอินเทอร์เน็ตหลุด ระบบยังคงทำงานอัตโนมัติได้ตามค่าที่ตั้งไว้ (เซฟตี้สัตว์เลี้ยง)
* 💡 **Status LED:** มีไฟ LED แสดงสถานะการเชื่อมต่อ WiFi และสว่างขึ้นเมื่อกำลังจ่ายอาหาร

---

## 🛠️ อุปกรณ์ที่ใช้ (Hardware Components)
1. **ESP32** - บอร์ดไมโครคอนโทรลเลอร์หลัก
2. **SHTC3** - เซ็นเซอร์วัดอุณหภูมิและความชื้น
3. **HC-SR04** - เซ็นเซอร์อัลตราโซนิกวัดระยะ (วัดระดับอาหาร)
4. **Servo Motor (SG90/MG996R)** - มอเตอร์เปิด/ปิดฝาจ่ายอาหาร
5. **Relay Module (Active LOW)** - โมดูลสวิตช์ควบคุมพัดลม
6. **พัดลม DC** - สำหรับระบายอากาศ
7. **LED (พร้อมตัวต้านทาน 220Ω)** - 2 ดวง สำหรับแสดงสถานะการจ่ายอาหารและสถานะWIFI

---

## 📌 การต่อสายวงจร (Pin Configuration)

| อุปกรณ์ (Component)      | ขา ESP32 (Pin)      | หมายเหตุ (Note) |
| :--- | :---: | :--- |
| **SHTC3 (Temp/Humid)**  | `SDA=21`, `SCL=22`  | เชื่อมต่อผ่าน I2C |
| **HC-SR04 (Ultrasonic)**| `TRIG=5`, `ECHO=18` | 
| **Servo Motor**         | `D13`               | PWM Control |
| **Relay (Fan)**         | `D14`               | สั่งงานแบบ Active LOW |
| **LED Action**          | `D27`               | ไฟติดเมื่อเซอร์โวทำงาน |
| **LED WiFi**            | `D2`                | ไฟสีน้ำเงินบนบอร์ด ESP32 |

---

## 💻 ไลบรารีที่จำเป็น (Required Libraries)
หากต้องการนำโค้ดไปคอมไพล์ใน Arduino IDE กรุณาติดตั้งไลบรารีดังต่อไปนี้:
* `WiFi.h` และ `WiFiClientSecure.h` (มีมากับ ESP32 Board Package)
* `WiFiManager` (tzapu)
* `Adafruit SHTC3 Library`
* `ESP32Servo` (Kevin Harrington)
* `Firebase ESP32 Client` (Mobizt)
* `HTTPClient.h`

---

## 🚀 วิธีการใช้งาน (Setup & Usage)
1. โคลน (Clone) หรือดาวน์โหลดโปรเจกต์นี้ลงเครื่อง
2. เปิดไฟล์ `.ino` ด้วย Arduino IDE และ index.html
3. แก้ไขข้อมูลในส่วนของการตั้งค่าให้เป็นของคุณเอง:
   * `API_KEY` และ `DATABASE_URL` (สำหรับ Firebase RealTime Database ใน .ino, index.html)
   * `BOT_TOKEN` และ `CHAT_ID` (สำหรับ Telegram ใน .ino)
4. อัปโหลดโค้ดลงบอร์ด ESP32
5. เมื่อบอร์ดเปิดขึ้นครั้งแรก ให้เชื่อมต่อ WiFi ชื่อ **SmartFeeder_Setup** (รหัสผ่าน: `12345678`) เพื่อตั้งค่าเครือข่ายอินเทอร์เน็ตให้บอร์ด
6. ดูผลลัพธ์ผ่าน Serial Monitor และหน้าเว็บควบคุม
